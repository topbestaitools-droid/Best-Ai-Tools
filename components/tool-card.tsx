import React, { Suspense } from 'react';\n\nconst ToolCard = ({ tool }) => {\n  const { name, imageUrl, description, link } = tool;\n\n  const handleError = (e) => {\n    e.target.src = 'https://via.placeholder.com/150'; // default placeholder\n  };\n\n  return (\n    <div className="tool-card">\n      <a href={link} target="_blank" rel="noopener noreferrer">\n        <Suspense fallback={<div>Loading...</div>}>\n          <img src={imageUrl} alt={name} onError={handleError} loading="lazy" />\n        </Suspense>\n        <h3>{name}</h3>\n        <p>{description}</p>\n      </a>\n    </div>\n  );\n};\n\nexport default ToolCard;\n